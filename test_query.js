const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log('Connecting to Prisma...'); const newUser = await prisma.user.create({ data: { name: 'Alice', email: 'alice_' + Date.now() + '@example.com' } }); console.log('Created user:', newUser); const users = await prisma.user.findMany(); console.log('All Users:', users); } main().finally(() => prisma.$disconnect());
